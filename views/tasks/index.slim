h1 Tasks

- @tasks.each do |task|
  p
    | #{task.id} - #{task.name} - #{task.page.url} - "#{task.selector}" - #{task.frequency} - #{task.last_updated}
    a href="/tasks/update/#{task.id}" Update now
    a href="/tasks/edit/#{task.id}" Edit
    a href="/tasks/delete/#{task.id}" Remove

p
  a href="/tasks/new" Add new task
